import type { SettingsMenuProps } from "./types"

export const mockSettingsMenuProps: SettingsMenuProps = {
  options: [
    { id: "account", label: "Account", href: "#" },
    { id: "support", label: "Support", href: "#" },
    { id: "signout", label: "Sign out", href: "#" },
  ],
}
