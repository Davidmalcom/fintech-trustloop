import type { MotivationCardProps } from "./types"

export const mockMotivationCardProps: MotivationCardProps = {
  mode: "quote",
  text: "Alone we can do so little; together we can do so much.",
  author: "H. Keller",
}
